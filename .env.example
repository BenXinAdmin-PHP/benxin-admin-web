# BenXinAdmin 管理后台 · 环境变量示例（入库，复制为 .env.development 后按需修改）
# 后端 admin 前缀地址，业务码风格 A：{ code, msg, data, request_id, timestamp }
# 后端本地默认端口 8801（§12：php think run -p 8801，避开本机 8000 占用）
VITE_API_BASE=http://127.0.0.1:8801/admin
